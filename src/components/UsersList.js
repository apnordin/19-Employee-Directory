import React from "react";

function UsersList(props) {

    return (
        <ul className="list-group">
            {/* {props.results.map(result => (
                <li key={result} className="list-group-item"> */}
                    <li>Placeholderfornow</li>
                {/* </li>
            ))} */}
        </ul>
    );
}

export default UsersList;